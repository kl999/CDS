pub struct Peer {
    
}
impl Peer {
    pub(crate) fn push_val(&self, key: String, value: String, client_id: u32) -> Result<(), String> {
        todo!()
    }
    
    pub(crate) fn work(&self, cds: &mut crate::cds::Cds) -> Result<(), String> {
        todo!()
    }
}